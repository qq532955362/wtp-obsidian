
```sql
SELECT
  SCHEMA_NAME
FROM
  information_schema.SCHEMATA
WHERE
  SCHEMA_NAME NOT IN ('__orca_in_polardb_m_', '__recycle_bin__', '', 'mysql', 'information_schema', 'performance_schema', 'sys', 'polardb_sys' -- 根据实际环境补充其他系统库
  )
ORDER BY
  SCHEMA_NAME;
SELECT
  CONCAT('DROP DATABASE IF EXISTS `', SCHEMA_NAME, '`;') AS drop_stmt
FROM
  information_schema.SCHEMATA
WHERE
  SCHEMA_NAME NOT IN ('__orca_in_polardb_m_', '__recycle_bin__', '', 'mysql', 'information_schema', 'performance_schema', 'sys', 'polardb_sys' -- 根据实际环境补充其他系统库
  )
ORDER BY
  SCHEMA_NAME;
  
-- DROP DATABASE IF EXISTS `atoto`;
-- DROP DATABASE IF EXISTS `atoto-backup`;
-- DROP DATABASE IF EXISTS `atoto-data-sync`;
-- DROP DATABASE IF EXISTS `atoto-file-center`;
-- DROP DATABASE IF EXISTS `atoto-gps-core`;
-- DROP DATABASE IF EXISTS `atoto-point`;
-- DROP DATABASE IF EXISTS `atoto-pricing-system`;
-- DROP DATABASE IF EXISTS `atoto-sandbox`;
-- DROP DATABASE IF EXISTS `atoto-user`;
-- DROP DATABASE IF EXISTS `driving-recorder`;
-- DROP DATABASE IF EXISTS `erp`;
-- DROP DATABASE IF EXISTS `flowable`;
-- DROP DATABASE IF EXISTS `knowledge_chat`;
-- DROP DATABASE IF EXISTS `mall-index`;
-- DROP DATABASE IF EXISTS `software_upgrade`;
-- DROP DATABASE IF EXISTS `timer-job`;
-- DROP DATABASE IF EXISTS `traccar`;
-- DROP DATABASE IF EXISTS `wordpressdb`;
-- DROP DATABASE IF EXISTS `xxl_job`;
```