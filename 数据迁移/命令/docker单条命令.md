```sh
## restart
docker restart $(docker ps -q --filter name=atoto-erp-flowable)  
docker restart $(docker ps -q --filter name=atoto-mall-index)  
docker restart $(docker ps -q --filter name=atoto-mall-product)  
docker restart $(docker ps -q --filter name=atoto-product-core)  
docker restart $(docker ps -q --filter name=atoto-product-ibook)  
docker restart $(docker ps -q --filter name=atoto-product-search)  
docker restart $(docker ps -q --filter name=atoto-software-upgrade)  
docker restart $(docker ps -q --filter name=atoto-user)  
docker restart $(docker ps -q --filter name=atoto-knowledge-chat)  
docker restart $(docker ps -q --filter name=atoto-file-center)  
docker restart $(docker ps -q --filter name=atoto-gps-core)  
docker restart $(docker ps -q --filter name=atoto-data-sync)  
docker restart $(docker ps -q --filter name=atoto-point-service)  
docker restart $(docker ps -q --filter name=atoto-common-file)  
docker restart $(docker ps -q --filter name=atoto-erp-core)  
docker restart $(docker ps -q --filter name=atoto-pricing-system)  
docker restart $(docker ps -q --filter name=atoto-data-crawler)  
docker restart $(docker ps -q --filter name=atoto-driving-recorder)  
docker restart $(docker ps -q --filter name=drivechat)  
docker restart $(docker ps -q --filter name=atoto-sandbox)

## stop
docker stop $(docker ps -q --filter name=atoto-erp-flowable)  
docker stop $(docker ps -q --filter name=atoto-mall-index)  
docker stop $(docker ps -q --filter name=atoto-mall-product)  
docker stop $(docker ps -q --filter name=atoto-product-core)  
docker stop $(docker ps -q --filter name=atoto-product-ibook)  
docker stop $(docker ps -q --filter name=atoto-product-search)  
docker stop $(docker ps -q --filter name=atoto-software-upgrade)  
docker stop $(docker ps -q --filter name=atoto-user)  
docker stop $(docker ps -q --filter name=atoto-knowledge-chat)  
docker stop $(docker ps -q --filter name=atoto-file-center)  
docker stop $(docker ps -q --filter name=atoto-gps-core)  
docker stop $(docker ps -q --filter name=atoto-data-sync)  
docker stop $(docker ps -q --filter name=atoto-point-service)  
docker stop $(docker ps -q --filter name=atoto-common-file)  
docker stop $(docker ps -q --filter name=atoto-erp-core)  
docker stop $(docker ps -q --filter name=atoto-pricing-system)  
docker stop $(docker ps -q --filter name=atoto-data-crawler)  
docker stop $(docker ps -q --filter name=atoto-driving-recorder)  
docker stop $(docker ps -q --filter name=drivechat)  
docker stop $(docker ps -q --filter name=atoto-sandbox)

## start 注意有 -a 需要看一下是否启动两个
docker start $(docker ps -a -q --filter name=atoto-erp-flowable)  
docker start $(docker ps -a -q --filter name=atoto-mall-index)  
docker start $(docker ps -a -q --filter name=atoto-mall-product)  
docker start $(docker ps -a -q --filter name=atoto-product-core)  
docker start $(docker ps -a -q --filter name=atoto-product-ibook)  
docker start $(docker ps -a -q --filter name=atoto-product-search)  
docker start $(docker ps -a -q --filter name=atoto-software-upgrade)  
docker start $(docker ps -a -q --filter name=atoto-user)  
docker start $(docker ps -a -q --filter name=atoto-knowledge-chat)  
docker start $(docker ps -a -q --filter name=atoto-file-center)  
docker start $(docker ps -a -q --filter name=atoto-gps-core)  
docker start $(docker ps -a -q --filter name=atoto-data-sync)  
docker start $(docker ps -a -q --filter name=atoto-point-service)  
docker start $(docker ps -a -q --filter name=atoto-common-file)  
docker start $(docker ps -a -q --filter name=atoto-erp-core)  
docker start $(docker ps -a -q --filter name=atoto-pricing-system)  
docker start $(docker ps -a -q --filter name=atoto-data-crawler)  
docker start $(docker ps -a -q --filter name=atoto-driving-recorder)  
docker start $(docker ps -a -q --filter name=drivechat)  
docker start $(docker ps -a -q --filter name=atoto-sandbox)


```